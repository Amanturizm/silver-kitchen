import React from 'react';
import { AppLayout } from '@/widgets/layout';
import AboutPage from '@/(pages)/about';

const Page = () => {
  return (
    <AppLayout>
      <AboutPage />
    </AppLayout>
  );
};

export default Page;
